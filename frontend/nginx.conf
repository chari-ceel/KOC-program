server {
    listen 3000;  # 监听 3000 端口（容器内）
    server_name localhost;

    root /usr/share/nginx/html;  # 指向 .next 目录
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;  # 支持 Next.js 路由
    }
}
